{
  "name": "lightmind-ui-framework",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A modern, backward-compatible UI framework built on shadcn/ui foundations",
  "scripts": {
    "dev": "node scripts/generate-registry.js && cd apps/gallery && npm run dev",
    "gallery": "node scripts/generate-registry.js && cd apps/gallery && npm run dev",
    "build": "node scripts/generate-registry.js && cd apps/gallery && npm run build",
    "build:cdn": "node build.js",
    "start": "cd apps/gallery && npm run start",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "clean": "rm -rf node_modules package-lock.json && npm install"
  },
  "devDependencies": {
    "@types/node": "^24.0.14",
    "prettier": "^3.6.2",
    "typescript": "^5.8.3"
  },
  "packageManager": "npm@11.4.2",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/lightmind-ui-framework.git"
  },
  "keywords": [
    "ui",
    "components",
    "react",
    "typescript",
    "tailwindcss",
    "shadcn",
    "design-system"
  ],
  "author": "Your Name",
  "license": "MIT"
}
